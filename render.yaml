databases:
  - name: quizzer-db
    plan: free
    databaseName: quizzer
    user: quizzer

services:
  - type: web
    plan: free
    name: quizzer
    runtime: python
    buildCommand: './build.sh'
    startCommand: 'uv run gunicorn quizzer.wsgi:application'
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: quizzer-db
          property: connectionString
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_DEBUG
        value: 'false'
      - key: WEB_CONCURRENCY
        value: '4'
